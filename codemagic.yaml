workflows:
  android-build:
    name: Compilar APK Android
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      android_gradle_plugin: 8.2.1
    scripts:
      - name: Criar projeto Android
        script: |
          flutter create --platforms=android --org com.wldecant .
      - name: Configurar SDK 36 no Gradle
        script: |
          printf "\nandroid.suppressUnsupportedCompileSdk=36\n" >> android/gradle.properties
          printf "flutter.compileSdkVersion=36\n" >> android/local.properties
      - name: Obter dependencias
        script: |
          flutter packages pub get
      - name: Compilar APK
        script: |
          flutter build apk --release --no-tree-shake-icons
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
    publishing:
      email:
        recipients:
          - wldecant@gmail.com
